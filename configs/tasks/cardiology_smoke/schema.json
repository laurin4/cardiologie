{
  "task": "cardiology_smoke",
  "description": "Smoke-Test für sternale Wundinfektion und Re-Operation erforderlich.",
  "language": "de",
  "fields": [
    {
      "name": "sternal_wound_infection",
      "type": "enum",
      "enum": ["Keine", "Oberflächlich", "Tief"],
      "required": true,
      "default": "Keine"
    },
    {
      "name": "reoperation_required",
      "type": "enum",
      "enum": ["Nein", "Ja", "Unbekannt"],
      "required": true,
      "default": "Unbekannt"
    },
    {
      "name": "information_sufficient",
      "type": "boolean",
      "required": true,
      "default": false
    },
    {
      "name": "reasoning",
      "type": "string",
      "required": true,
      "default": ""
    },
    {
      "name": "evidence_quotes",
      "type": "array",
      "required": true,
      "default": []
    }
  ]
}
